# Used by docker-compose.yml to bake public (browser-visible) config into the
# Next.js build. Copy this to `.env` in the project root before running
# `docker compose build`.

NEXT_PUBLIC_API_URL=http://localhost:5000
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=safcajaf
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=techarcade_preset

